---
import type { Project } from "../data/projects";

interface Props {
  project: Project;
}

const { project } = Astro.props;
const hasRealDemo = project.demoUrl && project.demoUrl !== "#";
---

<div class="project-card">
  <div class="project-image">
    {
      project.image ? (
        <img src={project.image} alt={project.name} />
      ) : (
        <div class="project-image-placeholder">
          <i class="fas fa-code" />
        </div>
      )
    }
    {project.inProgress && <span class="in-progress-badge">En construcción</span>}
    <div class="project-overlay">
      <div class="project-links">
        {
          hasRealDemo && (
            <a
              href={project.demoUrl}
              class="project-link"
              target="_blank"
              rel="noopener noreferrer"
            >
              <i class="fas fa-external-link-alt" />
            </a>
          )
        }
        {
          project.githubUrl && (
            <a
              href={project.githubUrl}
              class="project-link"
              target="_blank"
              rel="noopener noreferrer"
            >
              <i class="fab fa-github" />
            </a>
          )
        }
      </div>
    </div>
  </div>
  <div class="project-content">
    <h3>{project.name}</h3>
    <p>{project.description}</p>
    <div class="project-tech">
      {project.tech.map((t) => <span class="tech-tag">{t}</span>)}
    </div>
  </div>
</div>

<style>
  .in-progress-badge {
    position: absolute;
    top: 0.5rem;
    right: 0.5rem;
    z-index: 2;
    padding: 0.25rem 0.6rem;
    font-size: 0.7rem;
    font-weight: 600;
    color: #fff;
    background: #f59e0b;
    border-radius: 999px;
  }

  .project-image-placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2.5rem;
    color: var(--muted-foreground, #888);
    background: linear-gradient(135deg, rgba(120, 120, 120, 0.1), rgba(120, 120, 120, 0.2));
  }
</style>
